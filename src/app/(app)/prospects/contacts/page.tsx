import { PageStub } from "@/components/page-stub";

export default function ContactsPage() {
  return (
    <PageStub
      section="Prospects"
      title="Contacts"
      description="Grouped by company, sortable by score and tier. Names and titles listed free; select or select-all then bulk enrich reveals email and phone, with source and email quality shown."
    />
  );
}
