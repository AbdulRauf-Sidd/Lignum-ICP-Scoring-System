import { PageStub } from "@/components/page-stub";

export default function TriagePage() {
  return (
    <PageStub
      section="Prospects"
      title="Triage"
      description="Scored companies from your own runs land here first. Confirm the entity where the match is ambiguous, approve the proposed sector, resolve low-confidence and no-match flags before anything reaches the target list."
    />
  );
}
