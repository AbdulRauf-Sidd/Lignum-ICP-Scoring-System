import { PageStub } from "@/components/page-stub";

export default function TargetListPage() {
  return (
    <PageStub
      section="Prospects"
      title="Target list"
      description="Tabbed by ICP with the per-category score breakdown, total, tier, confidence and sector on each row. Sortable, filterable, exported status shown."
    />
  );
}
