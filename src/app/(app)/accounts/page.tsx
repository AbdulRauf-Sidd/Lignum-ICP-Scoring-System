import { PageStub } from "@/components/page-stub";

export default function AccountsPage() {
  return (
    <PageStub
      section="Accounts"
      title="Accounts"
      description="Companies at lifecycle_status = client, with firmographic data, an adverse-events feed, manually-entered qualitative ratings and Talent Insights, and an account health score."
    />
  );
}
