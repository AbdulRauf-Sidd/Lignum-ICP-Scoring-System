import { PageStub } from "@/components/page-stub";

export default function AnalyticsPage() {
  return (
    <PageStub
      section="Oversight"
      title="Analytics"
      description="Activity on trend charts by period, companies and contacts by sector. A standard user sees their own; an admin sees the overall view and the breakdown by user."
    />
  );
}
