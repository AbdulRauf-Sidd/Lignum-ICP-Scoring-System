import { PageStub } from "@/components/page-stub";

export default function UsagePage() {
  return (
    <PageStub
      section="Oversight"
      title="Usage & audit"
      description="Credits and reports as counts (Cognism, Creditsafe), and metered spend in pounds (Firecrawl, Exa, LLM). A log grouped by run, expandable to per-company line items."
    />
  );
}
